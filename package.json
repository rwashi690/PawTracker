{
  "name": "pawtracker",
  "version": "1.0.0",
  "dependencies": {
    "bootstrap": "^5.3.7",
    "react-bootstrap": "^2.10.10"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "start": "cd server && npm start",
    "heroku-postbuild": "cd pawtracker && npm install && npm run build && cd ../server && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
