const reportWebVitals = (onPerfEntry?: (metric: any) => void) => {
  if (onPerfEntry && typeof onPerfEntry === 'function') {
    // Temporarily disabled web-vitals reporting
  }
};

export default reportWebVitals;
